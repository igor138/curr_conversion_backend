export const add = (currency, destCurrency, amount) => {
  return 10
}

export const getStats = () => {
  return {
    popularDestCurrency: 'EUR',
    totalAmount: 1000,
    conversionsCount: 10
  }
}